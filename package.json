{
  "name": "repair-tracking-system",
  "version": "1.0.0",
  "description": "Repair tracking system",
  "main": "backend/src/app-simple.js",
  "scripts": {
    "start": "cd backend && node src/app-simple.js",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
