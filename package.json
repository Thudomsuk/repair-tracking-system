{
  "name": "repair-tracking-system",
  "version": "1.0.0",
  "description": "Repair tracking system with frontend and backend",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
